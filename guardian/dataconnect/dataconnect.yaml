specVersion: "v1beta"
serviceId: "guardian"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "guardian-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
